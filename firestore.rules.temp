rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads and writes (for adding admin user)
    match /{document=**} {
      allow read, write: true;
    }
  }
}
